// The map() method creates a new array by performing an operation on every element of an existing array


const numbers = [1, 2, 3, 4, 5];

const doubled = numbers.map(num => num * 2);

console.log(doubled);